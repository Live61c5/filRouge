<?php
// A quoi sert la fonction pregmatch()?
    // A vérifier si une chaîne de caractères correspond à une expression régulière
// A quoi sert la fonction trim()?
    // A supprimer les espaces en début et fin de chaîne
// A quoi sert la fonction stripslashes()?
    // A supprimer les antislashs d'une chaîne
// A quoi sert la fonction htmlspecialchars()?
    // A convertir les caractères spéciaux en entités HTML
// A quoi sert la fonction filter_var()?
    // A filtrer une variable avec un filtre spécifique
// A quoi sert la fonction empty()?
    // A vérifier si une variable est vide
// A quoi sert la fonction isset()?
    // A vérifier si une variable est définie
// A quoi sert la fonction header()?
    // A envoyer un en-tête HTTP
// A quoi sert la fonction exit()?
    // A quitter le script courant
// A quoi sert la fonction die()?
    // A afficher un message et quitter le script courant
// A quoi sert la fonction require_once()?
    // A inclure et exécuter un fichier
// A quoi sert la fonction include_once()?
    // A inclure un fichier
// A quoi sert la fonction session_start()?
    // A démarrer une session
// A quoi sert la fonction session_destroy()?
    // A détruire une session
// A quoi sert la fonction session_unset()?
    // A supprimer les variables de session
// A quoi sert la fonction session_id()?
    // A récupérer l'identifiant de session
// A quoi sert la fonction session_name()?
    // A récupérer le nom de la session
// A quoi sert la fonction session_save_path()?
    // A récupérer le chemin du dossier de stockage des sessions
// A quoi sert la fonction session_status()?
    // A récupérer l'état de la session
// A quoi sert la fonction session_write_close()?
    // A fermer la session
// A quoi sert la fonction setcookie()?
    // A définir un cookie
// A quoi sert la fonction date()? 
    // A récupérer la date et l'heure courante
// A quoi sert la fonction time()?
    // A récupérer le timestamp courant
// A quoi sert la fonction strtotime()?
    // A convertir une chaîne de caractères en timestamp
// A quoi sert la fonction date_default_timezone_set()?
    // A définir le fuseau horaire par défaut
// A quoi sert la fonction date_default_timezone_get()?
    // A récupérer le fuseau horaire par défaut 
// A quoi sert la fonction date_create()?
    // A créer un objet DateTime
// A quoi sert la fonction date_create_from_format()?
    // A créer un objet DateTime à partir d'une chaîne de caractères
// A quoi sert la fonction date_format()?
    // A formater une date
// A quoi sert la fonction date_modify()?   
    // A modifier une date
// A quoi sert la fonction date_add()?
    // A ajouter une durée à une date
// A quoi sert la fonction date_sub()?
    // A soustraire une durée à une date
// A quoi sert la fonction date_diff()? 
    // A calculer la différence entre deux dates
// A quoi sert la fonction date_timezone_get()?
    // A récupérer le fuseau horaire d'une date
// A quoi sert la fonction date_timezone_set()?
    // A définir le fuseau horaire d'une date
// A quoi sert la fonction date_timestamp_get()?
    // A récupérer le timestamp d'une date
// A quoi sert la fonction date_timestamp_set()?
    // A définir le timestamp d'une date
// A quoi sert la fonction date_interval_create_from_date_string()?
    // A créer un objet DateInterval à partir d'une chaîne de caractères
// A quoi sert la fonction date_interval_format()?  
    // A formater un objet DateInterval
// A quoi sert la fonction date_isodate_set()?
    // A définir une date à partir d'une année, d'une semaine et d'un jour de la semaine
// A quoi sert la fonction date_isodate_get()?  
    // A récupérer une date sous forme d'une année, d'une semaine et d'un jour de la semaine
// A quoi sert la fonction date_sunrise()?
    // A récupérer l'heure du lever du soleil
// A quoi sert la fonction date_sunset()?
    // A récupérer l'heure du coucher du soleil
// A quoi sert la fonction date_sun_info()? 
    // A récupérer les informations sur le lever et le coucher du soleil
// A quoi sert la fonction date_parse()?
    // A analyser une date
// A quoi sert la fonction date_parse_from_format()?
    // A analyser une date à partir d'un format
// A quoi sert la fonction date_get_last_errors()?
    // A récupérer les erreurs de la dernière analyse de date